// if maxcount is greater than n/2
        // return the corresponding element
        if (maxCount > n / 2)
        {
            cout << arr[index] << endl;
        }
        else
        {
            cout << "No majority element" << endl;
        }